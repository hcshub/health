<?xml version="1.0" encoding="UTF-8" ?>
        <!DOCTYPE mapper
                PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.golday.dao.CheckItemDao">
    <insert id="add" parameterType="checkItem">
        insert into t_checkItem (code,name,sex,age,price,type,remark,attention)
        values (#{code},#{name},#{sex},#{age},#{price},#{type},#{remark},#{attention});
    </insert>

    <select id="findCodeName" parameterType="String" resultType="checkItem">
        select * from t_checkItem
        <where>
            <if test="value != 'null' and value != '' and value != null">
                code like '%${value}%' or name like '%${value}%'
            </if>
        </where>
    </select>

    <select id="findCheck" parameterType="int" resultType="int">
        SELECT COUNT(1) FROM t_checkgroup_checkItem WHERE checkitem_id = #{id}
    </select>

    <delete id="delete" parameterType="int">
        delete from t_checkItem where id = #{id}
    </delete>

    <select id="showOne" parameterType="int" resultType="checkItem">
        select * from t_checkItem where id = #{id}
    </select>

    <update id="updateCheckItem" parameterType="checkItem">
        update t_checkItem set
        code = #{code},name = #{name},sex = #{sex},age = #{age},price = #{price},
        type = #{type},remark = #{remark},attention = #{attention}
        where id = #{id}
    </update>
</mapper>